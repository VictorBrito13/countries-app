import { FormatLargeNumbersPipe } from './format-large-numbers.pipe';

describe('FormatLargeNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLargeNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
